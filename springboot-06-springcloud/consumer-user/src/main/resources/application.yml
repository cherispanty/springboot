server:
  port: 9001

spring:
  application:
    name: consumer-user

eureka:
  instance:
    prefer-ip-address: true     #使用ip注册
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/